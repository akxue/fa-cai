@AGENTS.md

## Claude Code Notes

Claude Code reads this project memory file directly. The shared project instructions live in `AGENTS.md`; keep durable cross-agent guidance there so Codex and Claude stay aligned.

Use `docs/AGENT-ROLES.md` when coordinating specialized subagents or splitting implementation work.

